<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Example">
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/AppTheme.ActionBarTitle</item>
    </style>

    <style name="AppTheme.ActionBarTitle" parent="ActionBar.Solid.Example">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
